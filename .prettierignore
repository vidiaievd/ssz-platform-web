# Build output
.next/
out/
build/
coverage/
storybook-static/

# Test artefacts
playwright-report/
test-results/

# Vendored / tool-generated files we never hand-edit
public/mockServiceWorker.js
package-lock.json
openapi/spec.json

# Synced verbatim from ssz-platform/packages/shared-kernel — formatting it here would
# make `npm run kernel:check` fail on every format run. Format the source instead.
src/lib/shared-kernel/
