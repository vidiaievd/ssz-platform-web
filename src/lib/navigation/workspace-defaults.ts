/**
 * Default landing tab for a workspace shell (e.g. `/school/{slug}` -> `/school/{slug}/dashboard`).
 * Shared so every workspace index redirect (school, tutor, ...) points to the same segment.
 */
export const WORKSPACE_DEFAULT_SEGMENT = 'dashboard';
