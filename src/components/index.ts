// ─────────────────────────────────────────────
//  SSZ Component Library — barrel export
//  Import from "@/components/ssz" in your app
// ─────────────────────────────────────────────

// UI primitives (shadcn-extended)
export { Button, buttonVariants }           from "./ui/button";
export type { ButtonProps }                  from "./ui/button";

export { Badge, badgeVariants, StatusBadge } from "./ui/badge";
export type { LessonStatus }                 from "./ui/badge";

export {
  Card, CardHeader, CardTitle,
  CardDescription, CardBody, CardFooter,
}                                            from "./ui/card";

export { Input, Textarea, Field }            from "./ui/input";
export type { InputProps, TextareaProps }    from "./ui/input";

export { Alert, alertVariants, Toast }       from "./ui/alert";

export {
  Tabs, TabsList, TabsTrigger, TabsContent,
  Breadcrumb,
}                                            from "./ui/tabs";

export { Avatar }                            from "./ui/avatar";

export { ProgressBar, ProgressRing }         from "./ui/progress";

// Education-specific components

// Layout components
